import {
  articulateValueForTopicPrompt,
  articulateValuePrompt,
} from "../prompts"

export { articulateValueForTopicPrompt, articulateValuePrompt }

// TODO: Add some functions for values articulation here.

// Since we are doing articulation using vercel ai assistants, which is kind of custom, we will just export the prompts for now.
